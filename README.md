# VUE template from Linkstudio

## notification

template almost done

## Clean usage

client (in the directory)¡ª¡ª(cnpm OR npm)

`cnpm install`
`npm install --save html2canvas`(cnpm)
`npm install --save jspdf`(cnpm)
`cnpm run dev`

server (in the directory)

`composer install`
(if needed) `chmod 777 *`

## LOG

5.23 merge